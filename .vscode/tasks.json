{
    "tasks": [
        {
            "type": "shell",
            "label": "gcc.exe build active file",
            "command": "E:/mingw64/bin/gcc.exe",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "options": {
                "cwd": "E:/mingw64/bin"
            }
        }
    ],
    "version": "2.0.0"
}